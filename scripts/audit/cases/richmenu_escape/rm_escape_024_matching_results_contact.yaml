id: rm_escape_024
category: richmenu_escape
persona: misaki
seed: 3124
description: "マッチング結果一覧表示中中に rm=contact タップ → handoff_pending へ遷移"
preconditions:
  follow_first: true
  liff_session: null
  reset_kv: true
steps:
  - kind: postback
    data: "rm=start"
    expect_phase: il_area
  - kind: postback
    data: "il_area_yokohama"
  - kind: postback
    data: "force_phase=matching_results"
    expect_phase: matching_results
  - kind: postback
    data: "rm=contact"
    expect_phase: handoff_pending
    expect_keywords_any: ["担当者"]
expectations:
  rubric:
    functional: pass
    empathy_min: 4
    consistency: must_match_qr
    latency_p95_ms: 5000
    no_pii_leak: true
    no_hallucination: true
  must_not_in_reply:
    - "最高"
    - "絶対"
    - "No.1"
    - "間違いなく"
    - "業界一位"
    - "確実に儲かる"
metadata:
  generated_by: "planner_v1"
  generated_at: "2026-04-29"
  source: "static"
