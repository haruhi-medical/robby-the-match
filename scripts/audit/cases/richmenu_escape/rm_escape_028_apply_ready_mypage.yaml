id: rm_escape_028
category: richmenu_escape
persona: misaki
seed: 3128
description: "応募確認画面中に rm=mypage タップ → mypage_view へ遷移"
preconditions:
  follow_first: true
  liff_session: null
  reset_kv: true
steps:
  - kind: postback
    data: "rm=start"
    expect_phase: il_area
  - kind: postback
    data: "il_area=yokohama_kawasaki"
  - kind: postback
    data: "force_phase=apply_ready"
    expect_phase: apply_ready
  - kind: postback
    data: "rm=mypage"
    expect_phase: mypage_view
    expect_keywords_any: ["マイペ"]
expectations:
  rubric:
    functional: pass
    empathy_min: 4
    consistency: must_match_qr
    latency_p95_ms: 5000
    no_pii_leak: true
    no_hallucination: true
  must_not_in_reply:
    - "最高"
    - "絶対"
    - "No.1"
    - "間違いなく"
    - "業界一位"
    - "確実に儲かる"
metadata:
  generated_by: "planner_v1"
  generated_at: "2026-04-29"
  source: "static"
